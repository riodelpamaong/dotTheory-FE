{
  "name": "dottheory-fe",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tailwindcss -i ./css/main.css -o ./dist/main.css --watch",
    "sass-dev": "sass --watch scss:css",
    "prod": "sass scss:css --style compressed",
    "format": "prettier --write '**/*.html' '**/*.scss'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.8",
    "prettier-plugin-tailwindcss": "^0.3.0",
    "tailwindcss": "^3.3.2"
  }
}
